import Placeholder from "@/components/Placeholder";

export default function AgentsPage() {
  return (
    <Placeholder
      icon="ti-users"
      title="Field agents"
      description="Manage field agents — onboard new collectors, reset PINs, and deactivate accounts that should no longer collect."
    />
  );
}
